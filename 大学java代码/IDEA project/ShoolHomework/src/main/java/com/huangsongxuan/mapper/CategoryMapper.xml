<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huangsongxuan.mapper.CategoryMapper">
<!--there are sqls that can help us to find data we want-->
    <select id="findCategoryWithProduct" parameterType="Integer"
            resultMap="CategoryWithProductResult">
        select c.*,p.id as category_id,p.goodsname,p.price
        from category c,product p
        where c.id =p.category_id
        and c.id=#{id}
    </select>
<!--these mappings are the same with the class of pojo-->
    <resultMap id="CategoryWithProductResult" type="com.huangsongxuan.pojo.Category">
        <id property="id" column="id"/>
        <result property="typename" column="typename"/>
        <collection property="productList" ofType="com.huangsongxuan.pojo.Product">
            <id property="id" column="category_id"/>
            <result property="goodsname" column="goodsname"/>
            <result property="price" column="price"/>
        </collection>
    </resultMap>
</mapper>