<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xmcu.pojo.User">
    <select id="findbyId" parameterType="int"
            resultType="User">
	 	select * from mybatis.Users where uid = #{id}
	</select>
	<insert id="updatebook" parameterType="com.xmcu.pojo.User">
		update mybatis.Users
		set uid=#{uid},uname=#{uname},uage=#{uage}
		where uid=#{uid}
	</insert>
	<delete id="deleteUser" parameterType="int">
        delete from mybatis.Users where uid=#{uid};
    </delete>
</mapper>